[project]
    name = "magma"
    dynamic = ["version", "readme"]
    # TODO: increase minumun python version
    requires-python = ">=3.7"

    dependencies = [
        "rich",
    ]

    description = "Some tools to help manage the obsidian vault"
    authors = [
        {name = "Cesar Nunez Rodriguez", email = "cesarnr21@gmail.com" }
    ]

# set up packaging
[build-system]
    requires = ["setuptools"]
    build-backend = "setuptools.build_meta"

[tool.setuptools]
    packages = ["magma"]

[tool.setuptools.dynamic]
    version = {attr = "magma.__version__"}  # required
    readme = {file = ["README.md"], content-type = "text/plain"}

[project.scripts]
    codespell-improved = "magma.codespell_improved:codespell_skip_gitignore"


[project.optional-dependencies]
    dev = [
        "isort",
        "codespell",
        "pre-commit",
        "pylint"
    ]

    test = [
        "pytest",
        "coverage",
        "pytest-cov"
    ]

[tool.coverage.run]
    branch= true
    source = ["magma/"]
    command_line = "-m pytest -rA --doctest-modules --junitxml=pytest.xml"

[tool.pylint]
    [tool.pylint.MAIN]
    load-pluggins = [
        "pylint.extensions.typing",
        "pylint.extensions.docparams",
    ]
    exit-zero = true
    max-line-length = 150

    [tool.pylint.messages_control]
    disable = [
        "loggin-not-lazy",
        "missing-module-docstring",
        "import-error",
        "unspecified-encoding",
    ]

    [tool.pylint.REPORTS]
    # omit from the similarity reports
    ignore-comments = 'yes'
    ignore-docstring = 'yes'
    ignore-imports = 'yes'
    ignore-signature = 'yes'
    min-similarity-lines = 4
